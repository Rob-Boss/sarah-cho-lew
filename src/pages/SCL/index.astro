---
// SCL Portfolio - Reserved for future development
import "../styles/global.css";
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Sarah Cho Lew - Director</title>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Sacramento&display=swap"
            rel="stylesheet"
        />
    </head>
    <body>
        <div class="coming-soon">
            <h1>Sarah Cho Lew</h1>
            <p class="tagline">Director & Visual Storyteller</p>
            <div class="status">
                <span class="dot"></span>
                Portfolio Coming Soon
            </div>
            <a href="/labs" class="labs-link">Visit Design Labs →</a>
        </div>
    </body>
</html>

<style>
    body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #0a0a0f, #150a20);
    }

    .coming-soon {
        text-align: center;
        padding: 2rem;
    }

    h1 {
        font-family: "Sacramento", cursive;
        font-size: 5rem;
        font-weight: 400;
        margin: 0;
        background: linear-gradient(135deg, #8b5cf6, #ff007f);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .tagline {
        font-size: 1.25rem;
        color: rgba(255, 255, 255, 0.6);
        margin: 1rem 0 3rem;
        letter-spacing: 0.1em;
    }

    .status {
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.75rem 1.5rem;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 100px;
        color: rgba(255, 255, 255, 0.7);
        font-size: 0.9rem;
    }

    .dot {
        width: 8px;
        height: 8px;
        background: #98ff98;
        border-radius: 50%;
        animation: pulse 2s infinite;
    }

    @keyframes pulse {
        0%,
        100% {
            opacity: 1;
        }
        50% {
            opacity: 0.4;
        }
    }

    .labs-link {
        display: inline-block;
        margin-top: 2rem;
        color: #8b5cf6;
        text-decoration: none;
        font-size: 0.9rem;
        transition: color 0.3s ease;
    }

    .labs-link:hover {
        color: #ff007f;
    }

    @media (max-width: 768px) {
        h1 {
            font-size: 3rem;
        }
    }
</style>
